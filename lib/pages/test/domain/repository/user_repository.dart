import '../models/SaveUserNameParams.dart';
import '../models/user_model.dart';

class UserRepository {
  saveName({required SaveUserNameParams param}) {
    throw UnimplementedError();
  }

  Future<UserName> getName() {
    throw UnimplementedError();
  }
}
